#include "main.h"
/**
 * Description: print _alphabets - function that prints alphabets in lowercase
 *
 * Return: always 0
 */


void print_alphabet_x10(void);
{
char i;

for (i = 'a'; i <= 'z'; i++)
_putchar(i);
_putchar('\n');
return (0);
}
